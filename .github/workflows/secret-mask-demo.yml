name: Workflow Dispatcher

on: 
  push:
    branches: [main]

jobs:
  dispatcher:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar otro workflow
        run: |
          echo "Disparando workflow-receiver..."
          # Simular evento de disparo
          echo "Workflow disparado!"
